package com.myfoundation.school.heropanel;

/**
 * Background-image focus hint. Mapped to {@code object-position} on the public render so
 * an image of a person's face can stay centred when the hero reflows on mobile.
 */
public enum BackgroundFocus {
    CENTER,
    LEFT,
    RIGHT,
    TOP,
    BOTTOM
}
